Put your background photo here named exactly:

    background.jpg

Full path: public/background.jpg

It will be used as a blurred, frosted-glass background across the whole app.
A JPG, PNG, or WEBP works — if you use a different name/format, ask and the
reference in app/globals.css (url("/background.jpg")) can be updated to match.
No server restart needed; just refresh the page after adding the file.
